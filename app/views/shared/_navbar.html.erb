  <% if user_signed_in? %>

    <div class="navbar_signed_in ">
      <div class="container d-flex flex-column justify-content-between">
        <div class="logo">
          <%= link_to "", root_path, class: "" do %>
            <%= image_tag "SPORTEWS.png" %>
          <% end %>
        </div>

        <div class="bouttons-nav-loggin d-flex justify-content-center align-items-center">
          <div class="">
            <%= link_to "Se connecter", new_user_session_path, class: "btn-primary" %>
          </div>
          <div class="">
            <%= link_to "S'inscrire", new_user_registration_path, class: "btn-primary" %>
          </div>
        </div>

      </div>

    </div>

    <% else %>

    <div class="navbar_signed_out">
      <div class="container d-flex justify-content-between">
        <div class="logo">
          <%= link_to "", class: "" do %>
            <%= image_tag "SPORTEWS.png" %>
          <% end %>
        </div>

        <div class="bouttons-nav-loggout d-flex align-items-center justify-content-center">
          <div class="me-1">
            <%=link_to "Se connecter", new_user_session_path, class: "btn-connecter" %>
          </div>
          <div class="ms-1">
            <%=link_to "S'inscrire", new_user_registration_path, class: "btn-inscrire" %>
          </div>
        </div>

      <% end %>
      </div>

    </div>
